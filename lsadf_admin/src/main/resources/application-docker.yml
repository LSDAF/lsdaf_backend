db:
  url: ${DB_URL_DOCKER}

logging:
  config: file:logback-docker.xml

cache:
  redis:
    host: ${REDIS_HOST_DOCKER}

keycloak:
  url: ${KEYCLOAK_ADMIN_URI_DOCKER}
  admin-url: ${KEYCLOAK_ADMIN_URI_DOCKER}

