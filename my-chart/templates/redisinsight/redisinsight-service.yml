apiVersion: v1
kind: Service
metadata:
  name: redisinsight
  labels:
    app: redisinsight
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8001
      nodePort: 31093
  selector:
    app: redisinsight