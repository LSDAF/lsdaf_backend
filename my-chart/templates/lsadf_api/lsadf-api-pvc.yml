apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lsadf-api-volume-claim
  labels:
    app: lsadf-api
spec:
  storageClassName: fast
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi